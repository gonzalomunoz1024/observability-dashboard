# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# Frontend dependencies
/frontend/node_modules
/.pnp
.pnp.js

# Frontend testing
/frontend/coverage

# Frontend production build
/frontend/build

# Backend
/backend/build
/backend/.gradle
/backend/bin

# Bundled local-artifacts JAR exceeds GitHub's 100 MB hard limit; rebuild
# locally via `local-artifacts/build.sh`. Source is committed; the bundle is not.
/local-artifacts/dashboard.jar
/local-artifacts/cli-server/node_modules

# misc
.DS_Store
.env.local
.env.development.local
.env.test.local
.env.production.local

npm-debug.log*
yarn-debug.log*
yarn-error.log*
